{
    "appName": "${project.artifactId}",
    "profiles": [ "nashorn" ],
    "jdbc": {
        "jdbc/work-presentation": {
            "url": "${WORK_PRESENTATION_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        },
        "jdbc/corepo": {
            "url": "${COREPO_POSTGRES_URL}",
            "maxSize": "${THREADS}"
        }
    },
    "hazelcast": {
        "clusterName": "${project.artifactId}-${SYSTEM_NAME}",
        "cacheConfig": {
        }
    }
}
